{
  "name": "wasm-lang-test",
  "version": "0.1.19",
  "description": "Performance comparison of C++, Go, and Rust in WebAssembly",
  "private": true,
  "workspaces": [
    "packages/*",
    "ui"
  ],
  "scripts": {
    "build:wasm": "npm run build --workspaces --if-present",
    "dev": "cd ui && npm run dev",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.6.0"
  }
}
